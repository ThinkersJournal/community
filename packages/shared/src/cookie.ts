/** Name of the session cookie set on successful authentication. */
export const SESSION_COOKIE_NAME = 'tj_session';

/** Domain scope for the session cookie, shared across all subdomains. */
export const COOKIE_DOMAIN = '.thinkersjournal.com';
